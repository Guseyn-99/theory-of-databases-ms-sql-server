# Практическая работа по БД — 11.09.2026

## Что делали на паре

По материалам пары:

1. Просмотрели структуру базы данных и вывели структуру таблиц.
2. Рассмотрели структуру таблиц `STUDENT` и `AUDITORIUM`.
3. Изменяли таблицу `STUDENT` с помощью `ALTER TABLE`:
   - добавили новый столбец `EMAIL` (`ALTER ADD`);
   - изменили тип столбца `INFO` (`ALTER COLUMN`);
   - добавили ограничение `DEFAULT` для `EMAIL` (`CONSTRAINT`);
   - удалили столбец `FOTO` (`ALTER DROP`).
4. Проверили структуру таблицы через системное представление `INFORMATION_SCHEMA.COLUMNS`.
5. Дополнительно разобрали SQL-запросы со слайдами 8 и 14: агрегатные подсчёты по таблицам и условную обработку количества мест в аудиториях.

## Файлы

- `пара по БД (11.09).docx` — исходный документ с подписями и скриншотами.
- `screenshots/` — отдельные скриншоты из документа.
- `sql/` — SQL-коды, использованные/зафиксированные в материалах пары.

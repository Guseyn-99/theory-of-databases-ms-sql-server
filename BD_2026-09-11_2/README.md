# Практическая работа по БД — 11.09.2026, 2 пара

## Что делали на второй паре

По материалам пары разобрали:

1. Область видимости переменных.
2. `IF-ELSE` и `CASE`.
3. `CASE` с таблицей `TEACHER`.
4. Цикл `WHILE` с обновлением данных через `UPDATE`.
5. Управление циклом `WHILE` с помощью `CONTINUE` и `BREAK`.
6. `TRY/CATCH` и `RAISERROR`.
7. `WAITFOR`.
8. Курсоры.
9. Хранимые процедуры.
10. Функции.

## Файлы

- `пара по БД (11.09) - 2 пара.docx` — исходный документ со скриншотами.
- `screenshots/` — отдельные скриншоты из документа.
- `sql/` — SQL-код по темам второй пары, переписанный со скриншотов.

> SQL-файлы сохранены как учебная фиксация кода со скриншотов. Они не заменяют исходный документ.
